$(function(){








  /*
  function drawKotodama(t){ //tはミリ秒値  0.8から1あたりをぐるぐるさせたい。
    t = Math.sin(t/100);//tはゆれうごく

    //var canvas_height = kotodama_canvas.height; 同じ
    // 4. Draw graphics.

    c.clearRect(0,0,width,height);

    kotodama.draw(c,t,{
      showBox      : $showBox.prop("checked") ,
      showAxis     : $showAxis.prop("checked") ,
      showForceVec : $showForceVec.prop("checked") ,
      showMomentVec: $showMomentVec.prop("checked") 
    });
  }
  */


});

//window.mozAnimationStartTime // Firefoxのみ対応している関数です。 その他のブラウザでは Date.now() などの関数を使うことができます。


